title: Exploring Rust Implementing Futures
email: dpeklak@gmail.com
description: > # this means to ignore newlines until "baseurl:"
  An attempt ro write a futures crate in Rust, and to learn the language along the way
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com

# Build settings
markdown: kramdown
theme: minima
gems:
  - jekyll-feed
exclude:
  - Gemfile
  - Gemfile.lock
